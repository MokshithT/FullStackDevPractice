const obj1={
    fName:'Mokshith',
    age:20,
    Address:'Karimnagar',
    Salary:100000
}

//object functions

//keys --> to print only keys
let keys = Object.keys(obj1)
console.log(keys);

//values --> to print values
let values = Object.values(obj1)
console.log(values);

let entries = Object.entries(obj1)
console.log(entries);

let hasProp = Object.has
console.log(hasProp);


